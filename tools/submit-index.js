#!/usr/bin/env node
const host='InvoiceGeneratorNow.com';const key='9ccb443a8a4f55665f11e194da46d671';const base=`https://${host}`;const paths=process.argv.slice(2);const urlList=(paths.length?paths:['/','/features.php','/pricing.php','/invoice_software_small_business.php','/quote_and_invoice_software.php','/invoice_generator_uk.php','/methodology.php']).map(path=>path.startsWith('http')?path:base+(path.startsWith('/')?path:'/'+path));fetch('https://api.indexnow.org/IndexNow',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({host,key,keyLocation:`${base}/${key}.txt`,urlList})}).then(async response=>{console.log(`IndexNow status: ${response.status}`);if(!response.ok)process.exitCode=1;}).catch(error=>{console.error(error.message);process.exitCode=1;});
